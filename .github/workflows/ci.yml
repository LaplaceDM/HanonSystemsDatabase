# # .github/workflows/ci.yml
# name: CI

# on:
#   push:
#     branches: [ main, master, dev ]
#   pull_request:

# jobs:
#   test:
#     name: Ruff + Django Checks + Pytest
#     runs-on: ubuntu-22.04

#     services:
#       sqlserver:
#         image: mcr.microsoft.com/mssql/server:2019-latest
#         env:
#           ACCEPT_EULA: "Y"
#           SA_PASSWORD: "YourStrong!Passw0rd"
#           MSSQL_PID: "Express"
#         ports:
#           - 1433:1433
#         # 不使用 health-cmd（该镜像不内置 sqlcmd）；我们在 steps 中主动等待

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"

#       - name: Install system deps (ODBC17 & mssql-tools18)
#         shell: bash
#         run: |
#           set -e
#           sudo apt-get update
#           sudo apt-get install -y curl gnupg2 apt-transport-https ca-certificates
#           sudo mkdir -p /etc/apt/keyrings
#           curl -fsSL https://packages.microsoft.com/keys/microsoft.asc \
#             | sudo gpg --dearmor --batch --yes -o /etc/apt/keyrings/microsoft.gpg
#           echo "deb [arch=amd64 signed-by=/etc/apt/keyrings/microsoft.gpg] https://packages.microsoft.com/ubuntu/22.04/prod jammy main" \
#             | sudo tee /etc/apt/sources.list.d/microsoft-prod.list >/dev/null
#           sudo apt-get update
#           sudo ACCEPT_EULA=Y apt-get install -y msodbcsql17 mssql-tools18 unixodbc-dev
#           echo "/opt/mssql-tools18/bin" >> $GITHUB_PATH
        


#       - name: Wait for SQL Server ready
#         shell: bash
#         run: |
#           set +e
#           for i in {1..60}; do
#             /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P 'YourStrong!Passw0rd' -No -C -Q "SELECT 1" >/dev/null 2>&1
#             if [ $? -eq 0 ]; then
#               echo "SQL Server is ready."
#               exit 0
#             fi
#             echo "Waiting for SQL Server to be ready ($i/60)..."
#             sleep 2
#           done
#           echo "SQL Server failed to start in time." >&2
#           exit 1

#       - name: Create DB & app user (match settings.py)
#         shell: bash
#         run: |
#           /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P 'YourStrong!Passw0rd' -No -C -Q "
#           IF DB_ID('hanonsystems_database') IS NULL
#             CREATE DATABASE hanonsystems_database;
#           "
#           /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P 'YourStrong!Passw0rd' -No -C -Q "
#           IF NOT EXISTS(SELECT * FROM sys.sql_logins WHERE name = 'abc')
#           BEGIN
#             CREATE LOGIN abc WITH PASSWORD='abc', CHECK_POLICY=OFF, CHECK_EXPIRATION=OFF;
#           END
#           "
#           /opt/mssql-tools18/bin/sqlcmd -S localhost -d hanonsystems_database -U sa -P 'YourStrong!Passw0rd' -No -C -Q "
#           IF NOT EXISTS(SELECT * FROM sys.database_principals WHERE name = 'abc')
#           BEGIN
#             CREATE USER abc FOR LOGIN abc;
#             EXEC sp_addrolemember N'db_owner', N'abc';
#           END
#           "

#       - name: Cache pip
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/pip
#           key: pip-${{ runner.os }}-${{ hashFiles('**/requirements*.txt') }}
#           restore-keys: |
#             pip-${{ runner.os }}-

#       - name: Install Python deps
#         shell: bash
#         run: |
#           python -m pip install --upgrade pip
#           # 先装你的项目依赖（如果存在）
#           if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
#           # 开发/CI依赖
#           if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; else pip install ruff pytest pytest-django; fi
#           # 确保 MS SQL 相关依赖存在（与你 settings.py 匹配）
#           pip install pyodbc mssql-django

#       - name: Ruff format (check only)
#         run: ruff format --check .

#       - name: Ruff lint
#         run: ruff check .

#       - name: Django system checks & migrations
#         shell: bash
#         run: |
#           python manage.py check
#           # 确保迁移文件已提交（不生成，只校验）
#           python manage.py makemigrations --check --dry-run
#           # 应用迁移到 CI 的 SQL Server
#           python manage.py migrate --noinput
#           # 静态文件（有则收集；无则忽略）
#           python manage.py collectstatic --noinput || true

#       - name: Run tests (pytest)
#         run: pytest

name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps (root and/or app)
        run: |
          python -m pip install --upgrade pip
          # Install a root requirements.txt if present
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          # Install an app-level requirements.txt if present
          if [ -f HanonSystemsApp/requirements.txt ]; then pip install -r HanonSystemsApp/requirements.txt; fi
          # Make sure ruff is available for the lint step
          pip install ruff

      - name: Ruff (lint)
        run: ruff check .

      - name: Ruff (format check)
        run: ruff format --check .

      - name: Django system check
        # <<< This is the important part: run in the folder that has manage.py
        working-directory: HanonSystemsApp
        # If your settings module is HanonSystemsApp.settings, you can set it explicitly (optional)
        env:
          DJANGO_SETTINGS_MODULE: HanonSystemsApp.settings
        run: python manage.py check --no-input

